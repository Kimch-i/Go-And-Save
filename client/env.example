# Copy to .env. Every VITE_ value ends up in the public JavaScript, so never put a key or password here.

# Only "false" turns demo mode off and calls the real API.
VITE_USE_MOCK_API=true

# The Express API's URL, no trailing slash. Ignored in demo mode.
VITE_API_BASE_URL=http://localhost:3000
